---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "workos_cors_origin Resource - workos"
subcategory: ""
description: |-
  Manages a WorkOS AuthKit CORS origin in the current WorkOS environment.
  CORS origins are immutable in the WorkOS API. Changing origin forces
  Terraform to replace the resource.
  Example Usage
  
  resource "workos_cors_origin" "production_web" {
    origin = "https://app.example.com"
  }
  
  Import
  AuthKit CORS origins can be imported using their WorkOS ID:
  
  terraform import workos_cors_origin.example cors_origin_01H00000000000000000000000
---

# workos_cors_origin (Resource)

Manages a WorkOS AuthKit CORS origin in the current WorkOS environment.

CORS origins are immutable in the WorkOS API. Changing `origin` forces
Terraform to replace the resource.

## Example Usage

```hcl
resource "workos_cors_origin" "production_web" {
  origin = "https://app.example.com"
}
```

## Import

AuthKit CORS origins can be imported using their WorkOS ID:

```shell
terraform import workos_cors_origin.example cors_origin_01H00000000000000000000000
```

## Example Usage

```terraform
resource "workos_cors_origin" "production_web" {
  origin = "https://app.example.com"
}

output "production_web_cors_origin_id" {
  value = workos_cors_origin.production_web.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `origin` (String) The CORS origin. Changing this value forces replacement.

### Read-Only

- `created_at` (String) The timestamp when the CORS origin was created (RFC3339 format).
- `id` (String) The unique identifier of the AuthKit CORS origin.
- `updated_at` (String) The timestamp when the CORS origin was last updated (RFC3339 format).
